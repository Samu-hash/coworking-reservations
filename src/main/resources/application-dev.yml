spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/coworking
    username: coworking
    password: coworking
  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true

logging:
  level:
    com.cuscatlan.coworking: DEBUG
    org.hibernate.SQL: DEBUG
